name: 'marketing_warehouse'
version: '1.0.0'
config-version: 2

profile: 'marketing_warehouse'

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
seed-paths: ["seeds"]
analysis-paths: ["analyses"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  marketing_warehouse:
    staging:
      +materialized: view
      +schema: stg
    marts:
      +materialized: table
      +schema: marts
